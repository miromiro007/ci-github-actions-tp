name: Hello CI
on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
  pull_request:
    branches: [ main ] 
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Dire bonjour
        run: echo "Hello CI depuis le runner"
      - name: Inspecter le dossier
        run: ls -la  